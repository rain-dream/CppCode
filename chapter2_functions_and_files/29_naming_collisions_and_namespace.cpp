# include <iostream>

/*
1. If the colliding identifiers are introduced into the same file, the result will be a compiler error. 
If the colliding identifiers are introduced into separate files belonging to the same program, 
the result will be a linker error.

2. The global namespace

3. The :: symbol is an operator called the scope resolution operator. 
(1). When an identifier includes a namespace prefix, the identifier is called a qualified name (限定名称).
*/