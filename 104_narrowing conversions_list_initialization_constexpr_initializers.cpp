#include <iostream>

// 10.4 Narrowing conversions, list initialization, and constexpr initializers
/*
1. Narrowing conversions
(1). In C++, a narrowing conversion is a potentially unsafe numeric conversion 
where the destination type may not be able to hold all the values of the source type.
*/

int main(){

    return 0;
}